yt-dlp>=2026.3.17
opencv-python>=4.13.0
numpy>=2.5.0
scipy>=1.17.1
scikit-image>=0.21.0
osmnx>=1.9.0
networkx>=3.6.1
shapely>=2.1.2
matplotlib>=3.11.0
pyproj>=3.6.0
geopandas>=1.1.4
contextily>=1.6.0          # real RGB satellite basemap tiles (esri source)
open3d>=0.19.0
plotly>=6.8.0
pytest>=7.4.0
tqdm>=4.68.2
pycolmap>=4.1.0

# --- optional GPU stack for the dense-splat (DA3) path --------------------
# These are NOT required for the core trajectory/aerial pipeline; install
# only if you have a CUDA-capable GPU and want to run `--use-da3`.
#
# pip install --extra-index-url https://download.pytorch.org/whl/cu121 \
#     torch torchvision
# pip install depth-anything-3 addict
# pip install gsplat   # optional, only for refining the DA3 cloud as 3DGS

